#include "main.h"

/**
 * print_line - draw a straight line
 * @n: the number character
 * Return: if n is 0 or less
 */
void print_line(int n)
{
	for (n = '_'; n <= '10'; n++)
	{	
		if (n <= '0');
		_putchar(n);
	}
		_putchar('\n');
}
